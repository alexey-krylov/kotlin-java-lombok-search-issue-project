Example of Java+Kotlin+Lombok+IDEA integration.

To reproduce search usages issue go to Foo.java and try to find usages of 'uuid' field.
It should find one usage in class FooService.kt, but it doesn't.